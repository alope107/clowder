#pragma once

#include "game.h"
class beep
{
public:
    // constructor

    int play(int currFrame); // returns frame number for the frame that play happened

private:
    // frame ref for events
    // volume and other settings
};